<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="click"
            type="android.view.View.OnClickListener" />
    </data>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            tools:context=".activity.BluetoothActivity">
            <TextView
                android:id="@+id/bluetooth1"
                style="@style/MyTextviewStyle"
                android:text="bluetooth1"
                android:onClick="@{click}"
                />
            <TextView
                android:id="@+id/bluetooth2"
                style="@style/MyTextviewStyle"
                android:text="bluetooth2"
                android:onClick="@{click}"
                />
            <TextView
                android:id="@+id/bluetooth3"
                style="@style/MyTextviewStyle"
                android:text="bluetooth3"
                android:onClick="@{click}"
                />
            <TextView
                android:id="@+id/bluetooth4"
                style="@style/MyTextviewStyle"
                android:text="bluetooth4"
                android:onClick="@{click}"
                />
            <TextView
                android:id="@+id/bluetooth5"
                style="@style/MyTextviewStyle"
                android:text="bluetooth5"
                android:onClick="@{click}"
                />
            <TextView
                android:id="@+id/bluetooth6"
                style="@style/MyTextviewStyle"
                android:text="bluetooth6"
                android:onClick="@{click}"
                />
            <TextView
                android:id="@+id/bluetooth7"
                style="@style/MyTextviewStyle"
                android:text="bluetooth7"
                android:onClick="@{click}"
                />
            <TextView
                android:id="@+id/bluetooth8"
                style="@style/MyTextviewStyle"
                android:text="bluetooth8"
                android:onClick="@{click}"
                />
            <TextView
                android:id="@+id/bluetooth9"
                style="@style/MyTextviewStyle"
                android:text="bluetooth9"
                android:onClick="@{click}"
                />
            <TextView
                android:id="@+id/bluetooth10"
                style="@style/MyTextviewStyle"
                android:text="bluetooth10"
                android:onClick="@{click}"
                />
            <TextView
                android:id="@+id/bluetooth11"
                style="@style/MyTextviewStyle"
                android:text="bluetooth11"
                android:onClick="@{click}"
                />
            <TextView
                android:id="@+id/bluetooth12"
                style="@style/MyTextviewStyle"
                android:text="bluetooth12"
                android:onClick="@{click}"
                />
            <TextView
                android:id="@+id/bluetooth13"
                style="@style/MyTextviewStyle"
                android:text="bluetooth13"
                android:onClick="@{click}"
                />
            <TextView
                android:id="@+id/bluetooth14"
                style="@style/MyTextviewStyle"
                android:text="bluetooth14"
                android:onClick="@{click}"
                />
            <TextView
                android:id="@+id/bluetooth15"
                style="@style/MyTextviewStyle"
                android:text="bluetooth15"
                android:onClick="@{click}"
                />
            <TextView
                android:id="@+id/bluetooth16"
                style="@style/MyTextviewStyle"
                android:text="bluetooth16"
                android:onClick="@{click}"
                />
        </LinearLayout>
    </ScrollView>
</layout>