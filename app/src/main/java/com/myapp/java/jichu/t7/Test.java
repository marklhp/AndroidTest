package com.myapp.java.jichu.t7;

class Test {
    /**
     * 请说出hashcode方法，equals方法，hashset hashmap之间的关系
     *
     * hashset底层使用hashmap实现，这个hashmap的key就是hashset放进去的对象，object作为value
     */
    String string=new String("");
}
